workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event' # i hate merge request pipelines
      when: never
    - when: always

stages:
  - build
  - test

build:
  image: registry.gitlab.com/pagekey/pkos
  stage: build
  script:
    - make all
  artifacts:
    paths:
      - pkos.bin
      - pkos.iso

test:
  stage: test
  needs:
    - build
  script:
    - echo TODO
